import React, { useState } from "react";
import { useSelector } from "react-redux";
// import setTargetValue from "../../actions"

const MenuPlayers = ({}) => {
    return(
        <div></div>
    )
  };
export default MenuPlayers;
